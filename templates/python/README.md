# Python Template

Template for Python development workspaces.

Planned defaults:

- Ubuntu base image
- Python 3
- pip and venv
- Build dependencies for native packages
- Common API ports: `8000`, `5000`, `8080`
