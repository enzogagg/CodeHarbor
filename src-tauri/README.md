# Tauri Backend

This directory is reserved for the future CodeHarbor Tauri backend.

Planned responsibilities:

- Detect Docker availability
- Generate Compose projects from templates
- Start, stop and delete environments
- Open localhost URLs in the system browser
- Store environment metadata
- Return logs and process status to the frontend

The planned backend language is Rust.
