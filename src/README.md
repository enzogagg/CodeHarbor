# Frontend

This directory is reserved for the future CodeHarbor desktop frontend.

Planned responsibilities:

- Render the environment list
- Provide the environment creation flow
- Call Tauri commands for Docker actions
- Display logs, status and local URLs
- Host the future terminal UI integration

The planned stack is React and TypeScript inside Tauri.
